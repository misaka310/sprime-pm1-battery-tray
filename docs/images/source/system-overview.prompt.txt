Create an extremely clear Windows utility flow diagram for a GitHub README, exactly 1536x1024, 3:2 landscape, white background, dark navy text, restrained blue, green, red, and teal accents, very large rounded cards, large Japanese labels, no logos, no watermark, no decorative clutter. Title: 「SPRIME PM1 Battery Tray のしくみ」. Use one dominant horizontal data flow and one small separate auxiliary panel. No crossed arrows, no long auxiliary wires, no tiny text.

MAIN FLOW, solid arrows, left to right:
「SPRIME PM1 マウス」 → 「2.4GHz USBレシーバー」 → 「hidapiで feature report 0x05 を取得」 → 「状態を判定」 → 「Windows 通知領域」.

Inside 「状態を判定」 list exactly four large states: 「残量 0〜99%」, 「充電中」, 「未接続 / スリープ」, 「通信エラー」.
Inside 「Windows 通知領域」 show exactly four large examples with labels: 「96　通常」, 「99+　満充電」, 「--　未接続」, 「!　エラー」.

Add one compact auxiliary panel away from the main arrows, titled 「補助機能」, containing exactly 「今すぐ更新」 and 「Windows自動起動」. Do not connect this panel to the notification-area card with long arrows. Add one separate short note near the HID acquisition card: 「HID読み取りは排他制御」.

Add one prominent note below the final card: 「常駐表示はWindows通知領域のみ　フローティング表示なし」. All labels must remain comfortably readable at 750px display width. Use no additional nodes or explanatory text.
